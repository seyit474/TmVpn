<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash ekranı — uygulama açılırken gösterilir -->
    <style name="Theme.TmVpn" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.TmVpn.Main</item>
    </style>

    <!-- Asıl uygulama teması — içerik Compose tarafından çizilir -->
    <style name="Theme.TmVpn.Main" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/brand_navy</item>
        <item name="android:navigationBarColor">@color/brand_navy</item>
        <item name="android:windowBackground">@color/brand_navy</item>
    </style>
</resources>
